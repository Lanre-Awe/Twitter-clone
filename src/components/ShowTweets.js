import classes from "./TweetCard.module.css";

const ShowTweets = () => {
  return (
    <div className={classes.showtweets}>
      <span className={classes.show}>Show Tweets</span>
    </div>
  );
};

export default ShowTweets;
